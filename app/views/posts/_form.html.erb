<%= form_for @post do |f| %>
  <%= f.label :subject, "Subject" %>
  <%= f.text_area :subject %>
  <%= f.label :content, "Content" %>
  <%= f.text_area :content %>
  <%= f.submit "Submit", data: { :disable_with => 'Submiting...' } %>
<% end %> 
